{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "command": "bash .claude/hooks/check-secrets-pretooluse.sh",
        "description": "Scan for secrets and AWS account info before writing files"
      }
    ]
  }
}
