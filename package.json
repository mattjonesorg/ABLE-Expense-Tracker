{
  "name": "able-tracker",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "playwright": "^1.58.2"
  }
}
