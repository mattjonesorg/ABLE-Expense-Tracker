# ABLE Tracker — Deployment Configuration
#
# Copy this file to .env.deploy and fill in your values.
# .env.deploy is in .gitignore and will NOT be committed.
#
# All values here are environment-specific. The app works with
# defaults (CloudFront domain, no custom domain) if left blank.

# Required
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012

# Anthropic API key (stored in Secrets Manager, not used directly)
# Create the secret: aws secretsmanager create-secret --name able-tracker/anthropic-api-key --secret-string "your-key"
ANTHROPIC_API_KEY_SECRET_NAME=able-tracker/anthropic-api-key

# Optional — Custom domain configuration
# Leave blank to use the default CloudFront domain (*.cloudfront.net)
CUSTOM_DOMAIN=
CERTIFICATE_ARN=
HOSTED_ZONE_ID=
HOSTED_ZONE_NAME=
